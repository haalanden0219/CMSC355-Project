class Location:
    def __init__(self, row, shelf, bin):
        self.row
        self.shelf
        self.bin
    def myFunc(self):
        print("Location is Row " + self.row + ", Shelf " + self.shelf + "Bin " + self.bin + ".")

